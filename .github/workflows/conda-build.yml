# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

name: Build conda package
on:
  push:

  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.CONFIG }}
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 1080
    strategy:
      fail-fast: false
      max-parallel: 50
      matrix:
        include:
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h15798f2b
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_hfb2a64c5
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_hb3140566
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_hb8e74644
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h398274cc
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_he9dab72a
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h350f4763
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h4c708427
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h813a7aff
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h944cd481
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h5dc062a2
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h0f32791e
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h29b37798
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_hf405086a
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h66f6d926
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpimpich
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_hf8b49cb6
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpinompi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h24e0da9c
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_version12.9kokkos_h3f333ef6
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempimpich
            CONFIG_SHORT: linux_64_cuda_compiler_versionNonekokkos_h01535f86
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempinompi
            CONFIG_SHORT: linux_64_cuda_compiler_versionNonekokkos_hc5b0e925
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempiopenmpi
            CONFIG_SHORT: linux_64_cuda_compiler_versionNonekokkos_hcf905c90
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-latest']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h95c73331
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_hbc3e8b51
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h24212d45
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_hdfdd0390
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h6879b5f0
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h77a210fc
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_hb106f706
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h6c429781
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_ha68f2b13
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_ha7304ad4
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_hf4b6fa69
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL100mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_ha27338e6
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h4cbdcce1
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_hb0cc0a4a
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archBLACKWELL120mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_ha7372743
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h8c80ec76
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h93b3d29c
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_version12.9k_h65717bdd
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempimpich
            CONFIG_SHORT: linux_aarch64_cuda_compiler_versionNonek_h8c9093c6
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempinompi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_versionNonek_hcb8b07c0
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempiopenmpi
            CONFIG_SHORT: linux_aarch64_cuda_compiler_versionNonek_hda2f260f
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: build_artifacts
            docker_run_args: 
            free_disk_space: max
            os: ubuntu
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['ubuntu-24.04-arm']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_64_mpimpich
            CONFIG_SHORT: osx_64_mpimpich
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15-intel']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_64_mpinompi
            CONFIG_SHORT: osx_64_mpinompi
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15-intel']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_64_mpiopenmpi
            CONFIG_SHORT: osx_64_mpiopenmpi
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15-intel']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_arm64_mpimpich
            CONFIG_SHORT: osx_arm64_mpimpich
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_arm64_mpinompi
            CONFIG_SHORT: osx_arm64_mpinompi
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15']
            tools_install_dir: ~/miniforge3
          - CONFIG: osx_arm64_mpiopenmpi
            CONFIG_SHORT: osx_arm64_mpiopenmpi
            STORE_BUILD_ARTIFACTS: True
            UPLOAD_PACKAGES: True
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
            free_disk_space: max
            os: macos
            pagefile_size: 0
            resize_partitions: False
            runs_on: ['macos-15']
            tools_install_dir: ~/miniforge3
    steps:

    - name: Checkout code
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

    - name: Manage disk space
      env:
        FREE_DISK_SPACE: ${{ matrix.free_disk_space }}
        OS: ${{ matrix.os }}
      shell: bash
      run: |
        .scripts/free_disk_space.sh "${FREE_DISK_SPACE}"
    - name: Configure binfmt_misc
      if: matrix.os == 'ubuntu'
      shell: bash
      run: |
        if [[ "$(uname -m)" == "x86_64" ]]; then
          docker run --rm --privileged multiarch/qemu-user-static:register --reset --credential yes
        fi

    - name: Build on Linux
      id: build-linux
      if: matrix.os == 'ubuntu'
      env:
        CI: github_actions
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONDA_FORGE_DOCKER_RUN_ARGS: ${{ matrix.docker_run_args }}
        CONFIG: ${{ matrix.CONFIG }}
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        DOCKER_IMAGE: ${{ matrix.DOCKER_IMAGE }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        RATTLER_BUILD_COLOR: always
        RATTLER_BUILD_ENABLE_GITHUB_INTEGRATION: 'true'
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
      shell: bash
      run: |
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="${GITHUB_REF_NAME}"
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        ./.scripts/run_docker_build.sh

    - name: Build on macOS
      id: build-macos
      if: matrix.os == 'macos'
      env:
        CI: github_actions
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        RATTLER_BUILD_COLOR: always
        RATTLER_BUILD_ENABLE_GITHUB_INTEGRATION: 'true'
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
      shell: bash
      run: |
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="${GITHUB_REF_NAME}"
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        ./.scripts/run_osx_build.sh

    - name: Build on windows
      id: build-windows
      if: matrix.os == 'windows'
      shell: cmd
      run: |
        set "flow_run_id=github_%GITHUB_RUN_ID%"
        set "remote_url=https://github.com/%GITHUB_REPOSITORY%"
        set "sha=%GITHUB_SHA%"
        call ".scripts\run_win_build.bat"
      env:
        BUILD_PLATFORM: ${{ matrix.build_platform }}
        CI: github_actions
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        PYTHONUNBUFFERED: 1
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        RATTLER_BUILD_COLOR: always
        RATTLER_BUILD_ENABLE_GITHUB_INTEGRATION: 'true'
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}

    - name: Determine build outcome
      # this is to merge the status of the linux/osx/win builds into
      # something we can easily reuse during artefact generation
      id: determine-status
      if: ${{ always() }}
      shell: bash
      env:
        OS: ${{ matrix.os }}
      run: |
        if [[ "$OS" == "ubuntu" ]]; then
          STATUS=${{ steps.build-linux.outcome }}
        elif [[ "$OS" == "macos" ]]; then
          STATUS=${{ steps.build-macos.outcome }}
        elif [[ "$OS" == "windows" ]]; then
          STATUS=${{ steps.build-windows.outcome }}
        fi
        if [ -z "$STATUS" ]; then
          # steps that never ran will have empty status
          STATUS="cancelled"
        fi
        echo "status=$STATUS" >> $GITHUB_OUTPUT

    - name: Prepare conda build artifacts
      continue-on-error: true
      id: prepare-artifacts
      shell: bash
      # we do not want to trigger artefact creation if the build was cancelled
      if: ${{ always() && steps.determine-status.outputs.status != 'cancelled' && matrix.STORE_BUILD_ARTIFACTS }}
      env:
        CI: github_actions
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        CONFIG_SHORT: ${{ matrix.CONFIG_SHORT }}
        JOB_STATUS: ${{ steps.determine-status.outputs.status }}
        OS: ${{ matrix.os }}
      run: |
        export ARTIFACT_STAGING_DIR="$GITHUB_WORKSPACE"
        export CI_RUN_ID=$GITHUB_RUN_ID
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        # Archive everything in CONDA_BLD_PATH except environments
        # Archive the CONDA_BLD_PATH environments only when the job fails
        # Use different prefix for successful and failed build artifacts
        # so random failures don't prevent rebuilds from creating artifacts.
        if [ $JOB_STATUS == "failure" ]; then
          export BLD_ARTIFACT_PREFIX="conda_artifacts"
          export ENV_ARTIFACT_PREFIX="conda_envs"
        else
          export BLD_ARTIFACT_PREFIX="conda_pkgs"
        fi
        if [ $OS == "windows" ]; then
          pwsh -Command ". '.scripts/create_conda_build_artifacts.bat'"
        else
          ./.scripts/create_conda_build_artifacts.sh
        fi

    - name: Store conda build artifacts
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      if: ${{ always() && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true

    - name: Store conda build environment artifacts
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      # only relevant if build failed, see above
      if: ${{ always() && steps.determine-status.outputs.status == 'failure' && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true