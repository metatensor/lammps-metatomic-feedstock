# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

name: Build conda package
on:
  push:

  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.CONFIG }}
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 1080
    strategy:
      fail-fast: false
      matrix:
        include:
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpimpich
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h15798f2b
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpinompi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_hfb2a64c5
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archADA89mpiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_hb3140566
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpimpich
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_hb8e74644
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpinompi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h398274cc
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE80mpiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_he9dab72a
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpimpich
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h350f4763
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpinompi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h4c708427
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archAMPERE86mpiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h813a7aff
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpimpich
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_hf8b49cb6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpinompi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h24e0da9c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archHOPPER90mpiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h3f333ef6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archVOLTA70mpimpich
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h60c80412
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archVOLTA70mpinompi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h7a923247
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_version12.9kokkos_archVOLTA70mpiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9kokkos_h39b870eb
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempimpich
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonekokkos_h01535f86
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempinompi
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonekokkos_hc5b0e925
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_64_cuda_compiler_versionNonekokkos_archNonempiopenmpi
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonekokkos_hcf905c90
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h95c73331
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_hbc3e8b51
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archADA89mpiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h24212d45
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_hdfdd0390
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h6879b5f0
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE80mpiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h77a210fc
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_hb106f706
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h6c429781
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archAMPERE86mpiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_ha68f2b13
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h8c80ec76
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h93b3d29c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archHOPPER90mpiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h65717bdd
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archVOLTA70mpimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h3bab9ebf
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archVOLTA70mpinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_h38f2c3b3
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_version12.9kokkos_archVOLTA70mpiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9k_hf563a0e4
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempimpich
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonek_h8c9093c6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempinompi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonek_hcb8b07c0
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: linux_aarch64_cuda_compiler_versionNonekokkos_archNonempiopenmpi
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonek_hda2f260f
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma8
          - CONFIG: osx_64_mpimpich
            SHORT_CONFIG: osx_64_mpimpich
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-15-intel']
          - CONFIG: osx_64_mpinompi
            SHORT_CONFIG: osx_64_mpinompi
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-15-intel']
          - CONFIG: osx_64_mpiopenmpi
            SHORT_CONFIG: osx_64_mpiopenmpi
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-15-intel']
          - CONFIG: osx_arm64_mpimpich
            SHORT_CONFIG: osx_arm64_mpimpich
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-latest']
          - CONFIG: osx_arm64_mpinompi
            SHORT_CONFIG: osx_arm64_mpinompi
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-latest']
          - CONFIG: osx_arm64_mpiopenmpi
            SHORT_CONFIG: osx_arm64_mpiopenmpi
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['macos-latest']
    steps:

    - name: Manage disk space
      if: matrix.os == 'ubuntu'
      run: |
        sudo mkdir -p /opt/empty_dir || true
        for d in \
                 /opt/ghc \
                 /opt/hostedtoolcache \
                 /usr/lib/jvm \
                 /usr/local/.ghcup \
                 /usr/local/lib/android \
                 /usr/local/share/powershell \
                 /usr/share/dotnet \
                 /usr/share/swift \
                 ; do
          sudo rsync --stats -a --delete /opt/empty_dir/ $d || true
        done
        BROWSERS="firefox google-chrome-stable microsoft-edge-stable"
        BROWSERS_TO_REMOVE=$(dpkg --get-selections $BROWSERS 2>/dev/null | awk '{print $1}')
        sudo apt-get remove --purge -y $BROWSERS_TO_REMOVE

        sudo apt-get autoremove -y >& /dev/null
        sudo apt-get autoclean -y >& /dev/null
        sudo docker image prune --all --force
        df -h

    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

    - name: Build on Linux
      id: build-linux
      if: matrix.os == 'ubuntu'
      env:
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        DOCKER_IMAGE: ${{ matrix.DOCKER_IMAGE }}
        CI: github_actions
        CONDA_FORGE_DOCKER_RUN_ARGS: "${{ matrix.CONDA_FORGE_DOCKER_RUN_ARGS }}"
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
      shell: bash
      run: |
        if [[ "$(uname -m)" == "x86_64" ]]; then
          echo "::group::Configure binfmt_misc"
          docker run --rm --privileged multiarch/qemu-user-static:register --reset --credential yes
        fi
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        echo "::endgroup::"
        ./.scripts/run_docker_build.sh

    - name: Build on macOS
      id: build-macos
      if: matrix.os == 'macos'
      env:
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        CI: github_actions
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
      shell: bash
      run: |
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        ./.scripts/run_osx_build.sh

    - name: Build on windows
      id: build-windows
      if: matrix.os == 'windows'
      shell: cmd
      run: |
        set "flow_run_id=github_%GITHUB_RUN_ID%"
        set "remote_url=https://github.com/%GITHUB_REPOSITORY%"
        set "sha=%GITHUB_SHA%"
        call ".scripts\run_win_build.bat"
      env:
        # default value; make it explicit, as it needs to match with artefact
        # generation below. Not configurable for now, can be revisited later
        CONDA_BLD_DIR: C:\bld
        MINIFORGE_HOME: D:\Miniforge
        PYTHONUNBUFFERED: 1
        CONFIG: ${{ matrix.CONFIG }}
        CI: github_actions
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}

    - name: Determine build outcome
      # this is to merge the status of the linux/osx/win builds into
      # something we can easily reuse during artefact generation
      id: determine-status
      if: ${{ always() }}
      shell: bash
      env:
        OS: ${{ matrix.os }}
      run: |
        if [[ "$OS" == "ubuntu" ]]; then
          STATUS=${{ steps.build-linux.outcome }}
        elif [[ "$OS" == "macos" ]]; then
          STATUS=${{ steps.build-macos.outcome }}
        elif [[ "$OS" == "windows" ]]; then
          STATUS=${{ steps.build-windows.outcome }}
        fi
        if [ -z "$STATUS" ]; then
          # steps that never ran will have empty status
          STATUS="cancelled"
        fi
        echo "status=$STATUS" >> $GITHUB_OUTPUT

    - name: Prepare conda build artifacts
      continue-on-error: true
      id: prepare-artifacts
      shell: bash
      # we do not want to trigger artefact creation if the build was cancelled
      if: ${{ always() && steps.determine-status.outputs.status != 'cancelled' }}
      env:
        CI: github_actions
        CONFIG: ${{ matrix.CONFIG }}
        SHORT_CONFIG: ${{ matrix.SHORT_CONFIG }}
        JOB_STATUS: ${{ steps.determine-status.outputs.status }}
        OS: ${{ matrix.os }}
      run: |
        export CI_RUN_ID=$GITHUB_RUN_ID
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export ARTIFACT_STAGING_DIR="$GITHUB_WORKSPACE"
        if [ $OS == "macos" ]; then
          export CONDA_BLD_DIR="${MINIFORGE_HOME:-${HOME}/miniforge3}/conda-bld"
        elif [ $OS == "windows" ]; then
          # this needs to be consistent with build step, see above
          export CONDA_BLD_DIR="C:\\bld"
        else
          export CONDA_BLD_DIR="build_artifacts"
        fi
        # Archive everything in CONDA_BLD_DIR except environments
        # Archive the CONDA_BLD_DIR environments only when the job fails
        # Use different prefix for successful and failed build artifacts
        # so random failures don't prevent rebuilds from creating artifacts.
        if [ $JOB_STATUS == "failure" ]; then
          export BLD_ARTIFACT_PREFIX="conda_artifacts"
          export ENV_ARTIFACT_PREFIX="conda_envs"
        else
          export BLD_ARTIFACT_PREFIX="conda_pkgs"
        fi
        if [ $OS == "windows" ]; then
          pwsh -Command ". '.scripts/create_conda_build_artifacts.bat'"
        else
          ./.scripts/create_conda_build_artifacts.sh
        fi

    - name: Store conda build artifacts
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      if: ${{ always() && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true

    - name: Store conda build environment artifacts
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      # only relevant if build failed, see above
      if: ${{ always() && steps.determine-status.outputs.status == 'failure' && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true